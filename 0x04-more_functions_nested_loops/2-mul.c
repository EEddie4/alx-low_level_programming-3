#include "main.h"
/**
 * mul - multiplies two integers and returns the result
 * @a: first integer
 * @b: second integer
 * Return: returns the result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
